// Home — the single screen. Thin server entry; all interactivity lives in the
// client <Dashboard />.

import { Dashboard } from '@/components/dashboard'

export default function HomePage() {
  return <Dashboard />
}
